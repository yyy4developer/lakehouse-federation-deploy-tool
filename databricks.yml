bundle:
  name: lakehouse_federation_demo

sync:
  include:
    - "notebooks/**"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: ${DATABRICKS_HOST}
      root_path: /Shared/lakehouse_federation_demo
