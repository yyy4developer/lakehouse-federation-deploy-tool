bundle:
  name: lakehouse_federation_demo

sync:
  include:
    - "notebooks/federation_demo_aws.sql"
    - "notebooks/federation_demo_azure.sql"
  exclude:
    - "terraform/**"
    - "scripts/**"

targets:
  dev:
    default: true
    workspace:
      root_path: ~/.bundle/${bundle.name}
